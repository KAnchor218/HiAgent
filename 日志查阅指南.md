# HiAgent 评测日志查阅指南

本文整理 HiAgent 评测过程中各类日志的**存放位置、文件结构、查阅方法**，以及如何把终端输出完整地保存到日志文件以便事后分析。

---

## 一、HiAgent 写出了哪些日志

每次运行 `agentboard/eval_main.py` 后，`--log_path` 指向的目录下会产生三类文件。以 `./logs/test_run/` 为例：

```
logs/test_run/
├── all_results.txt          # 汇总结果（跨任务的总成绩）
├── pddl.txt                 # 每个任务一行的摘要
└── logs/
    └── pddl.jsonl           # 每个任务的完整结构化轨迹（最有价值）
```

### 1. `pddl.txt`（一行摘要）

每个任务一行，格式：

```
[EXP] 0: [success_rate]: False, [progress_rate]: 0.5, [grounding_acc]: 0.8, [score_state]: [(9, 0.5)]
[EXP] 1: [success_rate]: False, [progress_rate]: 0.1667, [grounding_acc]: 0.7, [score_state]: [(4, 0.1667)]
```

字段含义：

| 字段 | 含义 |
|---|---|
| `success_rate` | True/False，任务是否通关 |
| `progress_rate` | 子目标完成比例（例如 2 个子目标完成 1 个 = 0.5） |
| `grounding_acc` | 动作"落地率"—— 在所有步骤中，动作被环境判定为合法的比例 |
| `score_state` | `[(步号, 当时的累计 progress_rate)]`，记录得分发生变化的时刻 |

**适合场景**：快速扫一眼各任务的最终表现，不用进文件里查细节。

### 2. `logs/pddl.jsonl`（完整结构化轨迹，重点）

这是由 `agentboard/utils/logging/logger.py` 里的 `TaskLogger` 在评测结束时写入的**结构化轨迹日志**，每个任务一个 JSON 对象，包含：

```
{
  "id": 0,
  "task_name": "blockworld",
  "goal": "The goal is ...",
  "difficulty": "easy",
  "usage": {"prompt_tokens": ..., "completion_tokens": ..., "total_tokens": ...},
  "is_done": false,
  "progress_rate": 0.5,
  "grounding_acc": 0.8,
  "score_change_record": [[9, 0.5]],
  "trajectory": {
    "Interaction Turn 0": {
      "Goal": "...",
      "Observation": "...",
      "Action": "...",
      "Progress Rate": 0,
      "Prompt": "当轮实际送进 LLM 的完整提示文本"
    },
    "Interaction Turn 1": {...},
    ...
  }
}
```

**适合场景**：
- 分析某一步模型说了什么、做了什么
- 复盘为什么任务失败
- 查看实际送进 LLM 的 prompt（包括 few-shot 示例、压缩后的历史等）
- 统计 token 使用量

### 注意：jsonl 文件的解析坑

**HiAgent 写出的 pddl.jsonl 不是标准 jsonl 格式** —— 多个 JSON 对象之间**没有换行分隔**，直接拼在一起。因此不能用以下常规方式读取：

```python
# 错误写法：会在第二个对象处 JSONDecodeError
with open("pddl.jsonl") as f:
    for line in f:
        obj = json.loads(line)
```

必须用增量解析：

```python
# 正确写法：用 raw_decode 逐个对象解析
import json
with open("pddl.jsonl") as f:
    txt = f.read()
dec = json.JSONDecoder()
i = 0
objs = []
while i < len(txt):
    while i < len(txt) and txt[i].isspace():
        i += 1
    if i >= len(txt):
        break
    obj, end = dec.raw_decode(txt, i)
    objs.append(obj)
    i = end
```

### 3. `all_results.txt`（汇总结果）

跨任务、跨难度的总成绩汇总，通常包含 easy/hard 分组的成功率和进度率均值。在小规模测试时可能为空或只有少量内容。

---

## 二、查看完整轨迹的工具：inspect_traj.py

为了避免每次手动解析 jsonl，仓库根目录下放了一个辅助脚本 `inspect_traj.py`（首次使用时已经创建好），封装了上述解析逻辑。

### 用法

```bash
cd /home/cxw/Hiagent/HiAgent

# 1) 列出所有 exp 的摘要（只打印 id、目标、progress_rate 等）
python inspect_traj.py logs/test_run/logs/pddl.jsonl

# 2) 查看指定 exp 的完整轨迹（逐轮打印 Goal / Observation / Action / Progress Rate）
python inspect_traj.py logs/test_run/logs/pddl.jsonl 0
python inspect_traj.py logs/test_run/logs/pddl.jsonl 1
```

### 输出示例

```
=== EXP 0 | blockworld | easy ===
goal: The goal is to satisfy the following conditions: b2 is on b1., b3 is on b2.
is_done=False  progress_rate=0.5  grounding_acc=0.8
score_change_record: [[9, 0.5]]
# turns: 10

--- Interaction Turn 0 ---
  Goal: The goal is to satisfy the following conditions: b2 is on b1., b3 is on b2.
  Observation: B1 is on the table. B2 is on b3. B3 is on b1. The b2 is clear. Your arm is empty.
  Action: unstack b3 b1
  Progress Rate: 0

--- Interaction Turn 1 ---
  Action: check valid actions
  Observation: Valid actions are: Unstack b2 from b3., check valid actions
  Progress Rate: 0
...
```

通过这种方式就能一目了然地看出模型每一步的决策、环境反馈、以及 progress_rate 的变化时机。

---

## 三、把终端输出也存到日志文件

HiAgent 默认只把**结构化结果**写进 `pddl.jsonl`/`pddl.txt`，但**终端打印的日志**（INFO、Warning、报错堆栈、LLM 调用重试信息等）不会被写入文件。调试阶段这些信息非常重要，需要手动保存。

### 方式 A：单次命令直接重定向（最简单）

```bash
EVALTASK=blocksworld python -u agentboard/eval_main.py \
    --cfg-path eval_configs/hiagent/blocksworld_test.yaml \
    --tasks pddl \
    --model xiaoai-gpt4o \
    --log_path ./logs/test_run \
    --max_num_steps 10 \
    --agent ContextEfficientAgentV2 \
    2>&1 | tee ./logs/test_run/terminal.log
```

关键点说明：

| 参数 | 作用 |
|---|---|
| **`python -u`** | 关闭 Python stdout 缓冲。不加这个参数，`tee` 会滞后显示或者等到程序结束才刷出来（因为 stdout 会被缓冲成 4KB/8KB 块写入）。调试期尤其重要。 |
| **`2>&1`** | 把 stderr（报错信息、torchvision/deepspeed 的 Warning、LLM API 错误）合并到 stdout，这样 log 里才会有完整的报错堆栈。 |
| **`| tee FILE`** | 把 stdout 同时输出到终端和文件，不会屏蔽终端输出。 |

如果**只想存文件、不想刷屏**：

```bash
... 2>&1 > ./logs/test_run/terminal.log
```

### 方式 B：封装成脚本 run_eval.sh（推荐）

作者提供的 `evaluate_model.sh` 没做日志重定向，仓库根目录下已经放了一个改造版 `run_eval.sh`，内容如下：

```bash
#!/usr/bin/env bash
# HiAgent 评测启动脚本（含终端日志存档）
# 用法：bash run_eval.sh [task_name]
#   task_name 可选：blocksworld | gripper | barman | tyreworld | ...
#   不传则默认 blocksworld

set -e

# ---------- 基本配置 ----------
export PROJECT_PATH="/home/cxw/Hiagent/HiAgent"
# 如果不需要代理就把下面两行注释掉
# export http_proxy="http://127.0.0.1:7890"
# export https_proxy="http://127.0.0.1:7890"

MODEL="xiaoai-gpt4o"
AGENT="ContextEfficientAgentV2"
STEP=10                      # 正式复现请改成 50
MEMORY_SIZE=100

TASK="${1:-blocksworld}"
export EVALTASK="$TASK"

CFG="eval_configs/hiagent/${TASK}_test.yaml"   # 正式复现去掉 _test
RUN_NAME="${AGENT}_${MODEL}_${TASK}_step${STEP}_$(date +%Y%m%d_%H%M%S)"
LOG_DIR="./logs/${RUN_NAME}"
mkdir -p "$LOG_DIR"
LOG_FILE="${LOG_DIR}/terminal.log"

echo "==> cfg     : $CFG"
echo "==> task    : $TASK"
echo "==> model   : $MODEL"
echo "==> log dir : $LOG_DIR"
echo "==> tee to  : $LOG_FILE"
echo

# ---------- 启动评测 ----------
cd "$PROJECT_PATH"
python -u agentboard/eval_main.py \
    --cfg-path "$CFG" \
    --tasks pddl \
    --model "$MODEL" \
    --log_path "$LOG_DIR" \
    --max_num_steps "$STEP" \
    --memory_size "$MEMORY_SIZE" \
    --agent "$AGENT" \
    2>&1 | tee "$LOG_FILE"

echo
echo "==> Done. Terminal log: $LOG_FILE"
echo "==> Structured log   : $LOG_DIR/logs/pddl.jsonl"
echo "==> Summary          : $LOG_DIR/pddl.txt"
```

使用方式：

```bash
cd /home/cxw/Hiagent/HiAgent

# 默认跑 blocksworld
bash run_eval.sh

# 跑其它 pddl 子任务（需要对应的 _test.yaml 和 data/pddl/XXX.jsonl）
bash run_eval.sh gripper
bash run_eval.sh barman
bash run_eval.sh tyreworld
```

每次运行都会在 `logs/` 下生成一个**带时间戳的独立目录**，避免多次运行互相覆盖：

```
logs/ContextEfficientAgentV2_xiaoai-gpt4o_blocksworld_step10_20260409_162500/
├── terminal.log             # 终端完整输出
├── all_results.txt
├── pddl.txt
└── logs/
    └── pddl.jsonl
```

---

## 四、日志查阅速查表

遇到不同问题时该看哪个文件：

| 你想知道的事 | 看哪里 | 命令 |
|---|---|---|
| 本次跑的最终成绩（成功率、progress） | `pddl.txt` | `cat logs/<run>/pddl.txt` |
| 某个任务每一步做了什么 | `pddl.jsonl` | `python inspect_traj.py logs/<run>/logs/pddl.jsonl <id>` |
| 送进 LLM 的真实 prompt 长什么样 | `pddl.jsonl` 的 `trajectory.*.Prompt` 字段 | 同上 |
| 本次一共用了多少 token | `pddl.jsonl` 的 `usage` 字段 | 用 inspect_traj 或自行解析 |
| LLM 调用失败 / 重试过几次 | `terminal.log` | `grep -iE 'retry\|error\|fail' logs/<run>/terminal.log` |
| Python 报错堆栈 | `terminal.log` | `grep -A 20 Traceback logs/<run>/terminal.log` |
| torchvision / deepspeed 的 Warning | `terminal.log` | `grep -i warn logs/<run>/terminal.log` |
| 环境 / 依赖加载问题 | `terminal.log`（开头部分） | `head -50 logs/<run>/terminal.log` |

---

## 五、为什么这样设计，而不是别的

### 为什么要用 `tee` 而不是 `>` 重定向

- **`>`** 只写文件，终端看不到任何进度，调试期等 20 分钟才发现卡在第一步会很难受。
- **`tee`** 既能实时看又能事后翻文件，是调试期最佳选择。

### 为什么要加 `python -u`

Python 的 stdout 在非交互模式下默认是行缓冲或块缓冲，通过管道传给 `tee` 时会被 libc 改成全缓冲（4KB/8KB）。不加 `-u` 会看到"什么都不动，然后突然一大坨"的现象。加了 `-u` 就能逐行实时刷新。

替代写法：`export PYTHONUNBUFFERED=1` 在脚本开头设一次也可以。

### 为什么现在不用 `nohup`

- **`nohup ... &`** 适合无人值守跑几小时的大实验
- 但现在是**调试期**，看实时输出比后台化更重要
- 正式复现（`STEP=50`、完整 10 个问题、gpt-4-turbo）要跑很久的时候，再把 `run_eval.sh` 的末尾改成 `nohup ... &` 或配合 `tmux`/`screen` 就行

### 为什么带时间戳目录

- 避免多次运行互相覆盖 `pddl.jsonl`
- 做对比实验时可以同时保留多组结果
- 代价：需要定期清理老的 `logs/` 占用的磁盘空间

---

## 六、常见问题

### Q1：我运行后 `pddl.jsonl` 格式报错 JSONDecodeError 怎么办

答：HiAgent 写出的 jsonl 多个对象之间没有换行，需要用 `json.JSONDecoder().raw_decode()` 增量解析。直接用 `inspect_traj.py` 就好，它内部已经处理了。

### Q2：`terminal.log` 为空或只有一两行

可能原因：
- 忘了加 `python -u`，Python 输出还被缓冲着没刷到 tee
- 忘了加 `2>&1`，所有内容都走 stderr 没进文件
- 程序还没运行完（输出还在缓冲里）

检查脚本或命令，确保 `python -u` 和 `2>&1` 都在。

### Q3：想看 LLM 的每次 API 调用耗时 / 重试

答：`terminal.log` 里会有 INFO 级别的日志，配合 `grep`：

```bash
grep -iE 'llm|retry|openai|api' logs/<run>/terminal.log
```

更精细的耗时需要改 `agentboard/llm/` 下对应后端（如 `gpt.py`）加 timing 日志。

### Q4：logs/ 目录越来越大怎么办

`pddl.jsonl` 里每个 turn 都保存了完整 prompt，跑大实验时一个文件就可能几十 MB。定期清理老的测试运行即可：

```bash
# 查看各次运行的大小
du -sh logs/*/

# 删除某一次运行（确认后再执行）
rm -rf logs/ContextEfficientAgentV2_xiaoai-gpt4o_blocksworld_step10_20260409_162500/
```

---

## 七、相关文件位置速查

| 文件 | 路径 | 作用 |
|---|---|---|
| 评测入口 | `agentboard/eval_main.py` | 程序入口 |
| TaskLogger 实现 | `agentboard/utils/logging/logger.py` | 写 pddl.jsonl / pddl.txt |
| Agent 日志打印 | `agentboard/utils/logging/agent_logger.py` | 终端 INFO 日志的格式 |
| 启动脚本 | `run_eval.sh` | 带 tee 的评测启动脚本 |
| 轨迹查看工具 | `inspect_traj.py` | 解析 pddl.jsonl 打印轨迹 |
| 作者原版脚本 | `evaluate_model.sh` | 作者提供的脚本，未做日志重定向 |
