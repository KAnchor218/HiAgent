# HiAgent 评测复现指南（复现时只需要将论文方法进行指标复现对其即可，baseline不需要进行指标对其，跑两次baseline用于和论文方法对比来找缺陷即可）

## 一、快速运行

所有环境统一通过 `run_eval.sh` 启动，传入任务名即可自动路由配置和参数：

```bash
bash run_eval.sh blocksworld   # PDDL - 积木世界
bash run_eval.sh gripper       # PDDL - 机械臂抓取
bash run_eval.sh barman        # PDDL - 调酒师（带 retrieval）
bash run_eval.sh tyreworld     # PDDL - 换轮胎
bash run_eval.sh jericho       # 文字冒险游戏（需先准备游戏文件，见下文）
```

脚本固定参数（如需修改请直接编辑 `run_eval.sh`）：

| 参数 | 默认值 | 说明 |
|---|---|---|
| `MODEL` | `xiaoai-gpt4-turbo` | LLM 名称，须在 cfg yaml 的 `llm:` 节下存在对应配置 |
| `AGENT` | `ContextEfficientAgentV2` | Agent 类型 |
| `STEP` | `30` | 最大交互步数 |
| `MEMORY_SIZE` | `100` | Agent 记忆槽数量 |

补充说明：当前论文复现口径以脚本传入的 `--max_num_steps` 为准。`eval_main.py` 会用命令行参数覆盖 YAML 中的 `run.max_num_steps`，因此即使某个 YAML 里写的是别的值，脚本里 `STEP=30` 时实际生效的仍然是 `30`。

日志自动写入 `logs/<AGENT>_<MODEL>_<TASK>_step<STEP>_<时间戳>/`，包含：

- `terminal.log` — 完整终端输出
- `logs/<task>.jsonl` — 每条样本的详细轨迹
- `<task>.txt` — 每条样本的指标概览行

---

## 二、各环境详细说明

### 1. Blocksworld

| 项目 | 内容 |
|---|---|
| **eval 参数** | `--tasks pddl` |
| **cfg 文件** | `eval_configs/hiagent/blocksworld.yaml` |
| **数据文件** | `data/pddl/blocksworld.jsonl` |
| **样本数** | `env_num_per_task: 20` |
| **Prompt** | `agentboard/prompts/VanillaAgent/pddl_vanilla_prompt.json` |
| **任务描述** | 用机械臂将若干积木堆叠成目标结构，是经典 PDDL 规划基准 |
| **前置条件** | 无，开箱即用 |

---

### 2. Gripper

| 项目 | 内容 |
|---|---|
| **eval 参数** | `--tasks pddl` |
| **cfg 文件** | `eval_configs/hiagent/gripper.yaml` |
| **数据文件** | `data/pddl/gripper.jsonl` |
| **样本数** | `env_num_per_task: 20` |
| **Prompt** | `agentboard/prompts/ContextAgent/pddl.json` |
| **任务描述** | 控制机器人手爪在多个房间之间搬运球，考查多步规划能力 |
| **前置条件** | 无，开箱即用 |

> Gripper 使用 ContextAgent 版 prompt，而非 VanillaAgent 版，上下文压缩策略不同。

---

### 3. Barman

| 项目 | 内容 |
|---|---|
| **eval 参数** | `--tasks pddl` |
| **cfg 文件** | `eval_configs/hiagent/barman.yaml` |
| **数据文件** | `data/pddl/barman.jsonl` |
| **样本数** | `env_num_per_task: 20` |
| **Prompt** | `agentboard/prompts/ContextAgent/pddl_wretrieval.json` |
| **任务描述** | 调酒师场景，需按配方混合饮料，动作空间大、依赖关系复杂 |
| **前置条件** | 无，开箱即用 |

> Barman 使用带 retrieval 的 prompt（`pddl_wretrieval.json`），会从示例库检索相关 few-shot 样本后再生成动作，是四个 PDDL 任务中 prompt 最复杂的。

---

### 4. Tyreworld

| 项目 | 内容 |
|---|---|
| **eval 参数** | `--tasks pddl` |
| **cfg 文件** | `eval_configs/hiagent/tyreworld.yaml` |
| **数据文件** | `data/pddl/tyreworld.jsonl` |
| **样本数** | `env_num_per_task: 10`，仅跑 index 2–9（共 8 条） |
| **Prompt** | `agentboard/prompts/ContextAgent/pddl.json` |
| **任务描述** | 换轮胎场景，需按正确顺序完成拧螺丝、换备胎等步骤，对动作顺序要求严格 |
| **前置条件** | 无，开箱即用 |

> `problem_index: [2, 3, 4, 5, 6, 7, 8, 9]` — index 0、1 已跳过，复现时与原始设置保持一致。

---

### 5. Jericho

| 项目 | 内容 |
|---|---|
| **eval 参数** | `--tasks jericho` |
| **cfg 文件** | `eval_configs/hiagent/blocksworld.yaml`（复用，其中含 jericho env 配置） |
| **数据文件** | `data/jericho/test.jsonl` |
| **游戏文件** | `data/jericho/z-machine-games-master/jericho-game-suite/`（需手动准备） |
| **游戏数量** | 20 款文字冒险游戏（905、acorncourt、zork1–3 等） |
| **Prompt** | `agentboard/prompts/ContextAgent/jericho.json` |
| **任务描述** | 基于 Z-machine 的文字冒险游戏，Agent 需理解自然语言描述的场景并发出文本指令推进剧情 |

> 论文复现中 Jericho 的有效步数口径也是 `max_step=30`；若配置文件中的 `run.max_num_steps` 与脚本不同，以脚本传入值为准。

**前置步骤（必须）：**

```bash
# 方式一：通过 pip 安装 jericho 时附带游戏文件
pip install jericho
python -c "import jericho; print(jericho.__file__)"
# 找到 jericho 包路径后，将 games/ 目录复制到项目中：
cp -r <jericho包路径>/games \
  data/jericho/z-machine-games-master/jericho-game-suite

# 方式二：从 GitHub 手动下载
# https://github.com/microsoft/jericho → z-machine-games-master.zip
# 解压到 data/jericho/
```

脚本启动时若游戏目录不存在会自动报错退出并打印提示。

更详细的配置来源、`game_goals` / `game_init_obs` / `solution_lengths` 含义、`test.jsonl` 中 `goal` / `subgoals` 的作用，以及完整调用链说明，见：

- `Jericho配置与调用链说明.md`

---

## 三、WebShop（需额外启动服务）

WebShop 目前**数据和环境代码就绪**，但正式评测尚未运行过。

| 项目 | 内容 |
|---|---|
| **eval 参数** | `--tasks webshop` |
| **cfg 文件** | `eval_configs/hiagent/blocksworld.yaml`（含 webshop env 配置） |
| **数据文件** | `data/webshop/test.jsonl`（251 条，index 0–250） |
| **服务地址** | `http://127.0.0.1:3000` |
| **环境代码** | `agentboard/environment/WebShop/` |

**运行前需先启动 WebShop 服务：**

```bash
cd agentboard/environment/WebShop
bash run_dev.sh          # 前台运行，或加 nohup 放后台
# 等待出现 "Products loaded." 后再启动评测
```

目前 `run_eval.sh` 不支持 webshop（无 case 分支），如需评测可直接调用 `eval_main.py`：

```bash
python -u agentboard/eval_main.py \
    --cfg-path eval_configs/hiagent/blocksworld.yaml \
    --tasks webshop \
    --model xiaoai-gpt4-turbo \
    --log_path ./logs/webshop_test \
    --max_num_steps 50 \
    --memory_size 100 \
    --agent ContextEfficientAgentV2
```

---

## 四、环境对照汇总

| 任务 | 脚本命令 | cfg yaml | `--tasks` 值 | 数据文件 | 需要外部服务 |
|---|---|---|---|---|---|
| Blocksworld | `bash run_eval.sh blocksworld` | `blocksworld.yaml` | `pddl` | `data/pddl/blocksworld.jsonl` | 否 |
| Gripper | `bash run_eval.sh gripper` | `gripper.yaml` | `pddl` | `data/pddl/gripper.jsonl` | 否 |
| Barman | `bash run_eval.sh barman` | `barman.yaml` | `pddl` | `data/pddl/barman.jsonl` | 否 |
| Tyreworld | `bash run_eval.sh tyreworld` | `tyreworld.yaml` | `pddl` | `data/pddl/tyreworld.jsonl` | 否 |
| Jericho | `bash run_eval.sh jericho` | `blocksworld.yaml` | `jericho` | `data/jericho/test.jsonl` | 需下载游戏文件 |
| WebShop | 手动调用（见上文） | `blocksworld.yaml` | `webshop` | `data/webshop/test.jsonl` | 需启动 Flask 服务 |

---

## 五、常见问题

**Q：换个模型怎么办？**  
编辑 `run_eval.sh` 中的 `MODEL` 变量，确保该名称在目标任务的 yaml 文件 `llm:` 节下有对应配置。

**Q：Jericho 报错 `game file not found`？**  
确认 `data/jericho/z-machine-games-master/jericho-game-suite/` 目录下存在 `.z5` / `.z8` 等游戏文件。

**Q：想用 wandb 可视化？**  
在 `eval_main.py` 调用中加上 `--wandb --project_name <项目名>` 参数，评测结果会自动上传到 wandb.ai 对应项目。

**Q：如何只跑部分样本？**  
编辑对应 yaml 文件中 `env.pddl.problem_index` 字段，指定要跑的样本下标列表，例如 `problem_index: [0, 1, 2]`。
