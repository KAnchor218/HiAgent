# HiAgent 任务交互与评测机制总览

本文用于快速查阅 HiAgent 中各类任务的交互方式、动作合法性判断、`success_rate`、`progress_rate`、`grounding_acc` 的来源，以及各任务对 `label_path` / 标注文件的实际使用方式。

本文基于当前仓库代码整理，重点覆盖当前评测链路中常用的任务：

- PDDL 系：`blocksworld`、`gripper`、`barman`、`tyreworld`
- Jericho
- BabyAI
- ALFWorld
- ScienceWorld
- WebShop
- WebArena / WebBrowse
- Tool 系：`tool-query`、`tool-operation`，包括 `todo`、`movie`、`weather`、`academia`、`sheet`

---

## 一、通用评测流程

大多数任务都遵循同一层外框：

1. `agentboard/eval_main.py` 根据 `--tasks` 加载对应 `Task`。
2. `Task.evaluate()` 遍历多个样本或环境。
3. 每个样本调用 `Task.evaluate_env()`。
4. `agent.run()` 根据当前 goal 和 observation 生成动作。
5. `env.step(action)` 执行动作，返回 observation、reward、done、info。
6. task 记录每步轨迹、reward 变化、grounding 情况。
7. 最后汇总：
   - `success_rate`: 每题是否成功的平均值
   - `progress_rate`: 每题最终 progress/reward 的平均值
   - `grounding_acc`: 每题动作有效率的平均值
   - `score_change_record`: 单题中 progress 首次上升的步号记录

通用日志输出位置见 `日志查阅指南.md`。需要注意的是，当前 `logs/*.jsonl` 实际不是标准一行一个 JSON，而是多个 JSON 对象连续拼接，解析时需要使用 `json.JSONDecoder().raw_decode()` 做增量解析。

---

## 二、指标含义总览

| 指标 | 通用含义 | 不同任务的差异 |
|---|---|---|
| `success_rate` | 任务是否最终完成 | PDDL 看目标 literal 是否全部满足；Jericho/BabyAI/ScienceWorld 多看 reward 是否达到 1 或 done；WebShop/WebArena 看页面/evaluator 分数；Tool 任务看 `finish`、reward 和最终输出 |
| `progress_rate` | 任务进展程度 | PDDL 是目标条件满足比例；Jericho/BabyAI/ScienceWorld/ALFWorld 多是标注子目标命中比例；WebShop 是页面隐藏 progress score；WebArena 是 evaluator progress score；Tool 是 ground-truth subgoal 覆盖率或状态 match score |
| `grounding_acc` | 动作落地率 | PDDL/BabyAI 由环境判断动作是否有效；ALFWorld/ScienceWorld 用 action 是否在 valid action space；WebArena 主要统计动作解析和浏览器执行错误；Tool 任务用 observation 是否为 `ERROR` |
| `score_change_record` | progress 上升的时刻 | 记录 `(step_id, progress_rate)`，用于分析何时取得关键进展 |

### `reward` 和 `progress_rate` 的关系

在这个项目里，`reward` 和 `progress_rate` 大多数时候表示的是同一个“任务进展量”，只是所在层次不同：

- `reward` 是环境在每一步 `env.step(action)` 后返回的分数
- `progress_rate` 是 task 层写日志、汇总指标时，对该分数的记录名称

最常见的写法是：

```python
state, reward, done, infos = env.step(action)
...
progress_rate = reward
```

因此在多数任务中，单题 episode 结束时都可以近似理解为：

```text
progress_rate = 本题最终记录到的 reward
```

但要注意，不同任务里的 `reward` 来源并不一样：

- PDDL：`reward` 是目标 literal 满足比例，并且通常取历史最大值
- Jericho：`reward` 是命中的标注 subgoal 数 / 总 subgoal 数
- BabyAI / ScienceWorld / ALFWorld：`reward` 多数也是子目标命中比例
- Tool：`reward` 多数是标准中间子目标覆盖率，`sheet` 是状态 match score
- WebArena：`reward` 就是 evaluator 给出的 progress score
- WebShop：这是一个例外，环境里同时有 `reward` 和 `sub_reward`
  - `reward` 更接近最终页面得分
  - `sub_reward` 才是中间进度分
  - task 层记录 `progress_rate` 时使用的是 `sub_reward`，不是最终 `reward`

所以更准确地说：

```text
reward = 环境内部定义的进度分或结果分
progress_rate = task 层最终拿来展示和汇总的“进度”字段
```

在绝大多数任务里，`progress_rate` 直接等于 `reward`；只有少数任务，例如 WebShop，会把环境里的另一个进度字段单独拿出来作为 `progress_rate`。

---

## 三、PDDL 系任务

### 适用任务

`blocksworld`、`gripper`、`barman`、`tyreworld` 都走同一个 task：

- 任务入口：`agentboard/tasks/pddl.py`
- 环境入口：`agentboard/environment/pddl_env/pddl_env.py`
- 配置示例：`eval_configs/hiagent/blocksworld.yaml`、`gripper.yaml`、`barman.yaml`、`tyreworld.yaml`

### 交互方式

PDDL 任务底层使用 `pddlgym` 环境。Agent 看到的是自然语言化后的状态和目标，但环境内部保存的是 PDDL literal。

一次交互大致是：

1. `PDDL.reset()` 调用 `pddlgym` 加载指定 problem。
2. 环境读取当前逻辑状态 `obs.literals` 和目标 `obs.goal.literals`。
3. `literal_to_text()` 把 literal 转成自然语言 observation 和 goal。
4. Agent 输出文本动作，例如 `stack b2 b3`。
5. `text_to_action()` 把文本动作解析成 PDDL `Literal`。
6. `pddlgym.env.step(action_literal)` 执行动作。
7. 环境根据新状态重新计算 progress 和 success。

### 动作合法性判断

普通动作必须满足两层条件：

- 文本能被解析成当前可用 predicate 和对象组成的 PDDL action。
- 执行动作后状态真的发生变化。

如果解析失败，会返回：

```text
The action is not valid and therefore takes no effect. Please check valid actions.
```

如果解析成功但前置条件不满足，`pddlgym` 会保持状态不变，代码也会判定无效：

```text
The action is not valid and therefore takes no effect. Please remember to satisfy the restriction of actions. You can also check valid actions.
```

特殊动作：

- `check valid actions`: 返回当前所有合法动作，算有效动作。
- `look around`: 主要用于 `barman`、`tyreworld`，返回当前完整状态，算有效动作。

### progress_rate 判断

PDDL 的 `progress_rate` 不是文本匹配，而是逻辑目标满足比例。

核心逻辑：

```python
satisfied = 0
for literal in goal_literals:
    if literal in obs_literals:
        satisfied += 1
progress = satisfied / len(goal_literals)
```

并且 reward 使用历史最大值：

```python
reward = max(self.reward, current_progress)
```

因此 PDDL 的 `progress_rate` 表示“本局历史最好进展”，不是当前瞬时状态。如果 agent 曾经满足了 3/4 个目标条件，后来又破坏了一个，日志里的 progress 仍可能保持 `0.75`。

### success_rate 判断

PDDL 的 success 看 `env.won`，即当前状态是否同时满足所有 goal literals。

简化逻辑：

```python
self.won = True
for literal in goal_literals:
    if literal not in obs_literals:
        self.won = False
```

单题成功必须满足：

- 环境执行到 `done=True`
- 且 `env.won=True`

Task 层最终记录的是 `env.won`，不是单纯的 `done`。

### label_path 的作用

PDDL 的 `data/pddl/*.jsonl` 中虽然有 `goal` 和 `subgoals` 字段，但当前评测代码只读取 `difficulty`。这些 `subgoals` 不参与 reward 或 success 判断。

换句话说：

- PDDL 评测不依赖 `Subgoal 1:` 这类文本格式。
- PDDL 的真实评分来自 `pddlgym` 内部状态和目标 literal。

### 例子

目标：

```text
b2 is on b3.
b3 is on b1.
```

初始状态大致是：

```text
B1 is on b3. B2 is on the table. B3 is on b2. The b1 is clear. Your arm is empty.
```

关键动作：

```text
unstack b1 b3
putdown b1
unstack b3 b2
stack b3 b1
pickup b2
stack b2 b3
```

当执行 `stack b3 b1` 后，状态满足 `b3 is on b1`，两个目标满足一个，因此 `progress_rate = 0.5`。

当执行 `stack b2 b3` 后，状态同时满足：

```text
b2 is on b3.
b3 is on b1.
```

此时 `progress_rate = 1.0`，`success=True`。

---

## 四、Jericho

### 入口文件

- 任务入口：`agentboard/tasks/jericho.py`
- 环境入口：`agentboard/environment/jericho_env.py`
- 标注文件：`data/jericho/test.jsonl`
- prompt：`agentboard/prompts/ContextAgent/jericho.json`

### 交互方式

Jericho 是文字冒险环境，底层用 `FrotzEnv` 执行文本命令。

一次交互大致是：

1. `Jericho.reset()` 加载指定游戏文件，例如 `905.z5`。
2. 环境用 `game_goals` 里的人工目标作为 agent goal。
3. 环境用 `game_init_obs` 里的人工清洗版开局文本作为初始 observation。
4. Agent 输出简单文本动作，例如 `take wallet`、`east`、`open front door`。
5. `env.step(action)` 发送给 Frotz。
6. Frotz 返回新 observation。
7. 环境用 observation 和 `obs_to_reward` 中的标注文本做匹配，更新 reward。

更完整地说，Jericho 的运行链路分为五层：

1. 启动脚本层  
   `bash run_eval.sh jericho` 不会读单独的 `jericho.yaml`，而是复用 `eval_configs/hiagent/blocksworld.yaml` 中的 `env.jericho` 配置。

2. task 配置层  
   `EvalJericho` 读取：
   - `game_name`
   - `game_dir`
   - `label_path`
   - `max_num_steps`（最终以脚本传入的 `--max_num_steps` 为准；当前论文复现口径为 `30`）

3. 样本构造层  
   对于 `game_name` 中的每个游戏，task 会生成一个 `env_config`，其中包括：
   - `game_file`
   - `difficulty`
   - `obs_to_reward`

4. 环境交互层  
   `Jericho.reset()` 初始化 `FrotzEnv`，并给 agent 提供手写 goal 与手写初始 observation。

5. 评分层  
   每一步环境都用当前 observation 去匹配剩余 `obs_to_reward`，据此更新 `reward/done/won`。

### `goal` 和 `init_obs` 实际来自哪里

Jericho 当前实际喂给 agent 的大目标和开局观察，不直接来自 `data/jericho/test.jsonl`，而来自环境代码中的两组手写映射：

- `game_goals`
- `game_init_obs`

这意味着：

- `test.jsonl.goal` 当前基本只是一份平行标注说明，没有直接进入 agent 的输入
- `test.jsonl.subgoals` 才是当前评测的核心打分依据

这样设计的好处是初始上下文更稳定，但缺点是数据文件里的 `goal` 和真实运行链路容易让人混淆

### 动作合法性判断

特殊动作：

- 包含 `check` 和 `action` 的动作，会被识别为 `check valid actions`。
- 该动作返回当前 `FrotzEnv.get_valid_actions()` 列表，算有效动作。

关于 `inventory`：

- 在 prompt 里，`Inventory` 被描述为“检查你当前携带的物品”
- 在配置里也有 `check_inventory: "inventory"`
- 但在当前 Jericho 环境实现中，`inventory` 没有被当作特殊动作单独处理
- 它只是一个普通游戏命令，和 `take wallet`、`open door` 一样，直接交给 `FrotzEnv.step(action)`

因此，`inventory` 的作用是：

- 帮助 agent确认自己当前拿着什么物品
- 便于决定下一步是否需要找钥匙、穿衣、携带工具等
- 它本身不直接加分，也不会被特殊计分

在 `905` 这种任务里，`inventory` 经常用于确认：

- 是否已经拿到了 `wallet`
- 是否已经拿到了 `keys`
- 是否仍穿着 `soiled clothing`
- 是否还戴着 `watch`

普通动作：

- 直接传给 Frotz 执行。
- 当前实现只通过 observation 中是否包含 `'[!'` 来判断动作无效。

这个判断很粗糙。很多真实失败动作不会返回 `'[!'`，例如：

```text
You'll have to get out of bed first.
That isn't a useful direction from here.
It seems ridiculous to put on fresh, clean clothes when you yourself are covered with mud and dried sweat.
```

这些在当前实现里可能仍被计为有效动作，因此 Jericho 的 `grounding_acc` 偏乐观。

### progress_rate 判断

Jericho 的 progress 使用 `data/jericho/test.jsonl` 中的 `subgoals`。

加载逻辑：

1. `EvalJericho.load_annotation()` 读取每行 JSON。
2. 如果 `subgoals` 是字符串，就按 `\n` 切成多条。
3. 每条 subgoal 放进环境配置 `obs_to_reward`。
4. 每一步用当前 observation 去匹配剩余 pattern。
5. 命中一个 pattern，`points += 1`，reward 变为 `points / num_obs_to_reward`。

匹配逻辑：

- observation 和 pattern 都去掉非字母数字字符。
- 然后判断 `pattern in observation`。

### success_rate 判断

如果 `obs_to_reward` 存在，Jericho 不直接使用游戏原生 reward 判断成功，而是：

- `reward == 1` 时 `done=True`
- `reward == 1` 时 `won=True`

也就是所有标注 subgoal 都匹配到，才算成功。

换句话说，Jericho 当前“成功”的定义不是：

- 游戏原生结束了

而是：

- 所有手写标注子目标都在 observation 中被命中过

如果游戏原生 `done=True`，但 `reward < 1`，task 层最终仍会把 success 记成 `False`。

### 当前发现的关键问题

当前 `data/jericho/test.jsonl` 的 subgoal 形如：

```text
Subgoal 1: You get out of bed.
Subgoal 2: Bathroom This is a far from luxurious but still quite functional bathroom...
```

但真实 observation 只会出现：

```text
You get out of bed.
Bathroom This is a far from luxurious but still quite functional bathroom...
```

由于代码没有去掉 `Subgoal 1:` 前缀，匹配时实际拿整行：

```text
Subgoal 1: You get out of bed.
```

去匹配 observation，所以即使 agent 真的触发了 `You get out of bed.`，reward 仍然不会上涨。

说明：

- 当前仓库代码已在 `agentboard/tasks/jericho.py` 的 `load_annotation()` 中加入前缀清洗
- 新运行的 Jericho 评测会先把 `Subgoal n:` 去掉，再送入 `obs_to_reward`
- 但历史日志如果是修复前跑出来的，仍然会保留旧结果

这解释了三次 Jericho 运行中：

- `success_rate = 0.0`
- `progress_rate = 0.0`
- `score_change_record = []`

离线去掉 `Subgoal n:` 前缀后重放三次轨迹，可以看到已有不少子目标实际被命中，说明全 0 主要是评分实现问题，不是 agent 完全没有进展。

### `905` 实例：一次真实 episode 的逐步轨迹与 reward 变化

下面用一次真实运行的 `905` episode 来说明当前 Jericho 是如何交互和评分的。该 episode 来自：

- `logs/ContextEfficientAgentV2_xiaoai-gpt4-turbo_jericho_step30_20260423_103634/logs/jericho.jsonl`

这一局在当前代码下的最终结果是：

- `success = False`
- `progress_rate = 0.0`
- `steps = 30`
- `score_change_record = []`

但如果仅修掉 `Subgoal n:` 前缀，按当前同样的 observation 匹配逻辑离线重放，该局实际上能命中 7 个子目标中的 3 个，修正后 progress 应为 `3 / 7 = 0.429`。

`905` 的标注子目标去掉前缀后如下：

1. `You get out of bed.`
2. `Bathroom This is a far from luxurious but still quite functional bathroom, with a sink, toilet and shower. The bedroom lies to the north.`
3. `You step into the shower, turn on the water, and within a few moments you feel like a new man. But no time to dawdle - you hop back out again and dry off in record time.`
4. `You put on the clean clothing.`
5. `You open the front door.`
6. `You climb inside and start up the engine.`
7. `You have left Las Mesas`

逐步轨迹如下：

| Step | Action | 原始 PR | 修正后 PR | 命中子目标 | 说明 |
|---|---|---:|---:|---|---|
| 0 | `take wallet` | 0.000 | 0.000 | - | 拿钱包，不加分 |
| 1 | `take keys` | 0.000 | 0.000 | - | 拿钥匙，不加分 |
| 2 | `take telephone` | 0.000 | 0.000 | - | 实际上是接电话，不加分 |
| 3 | `inventory` | 0.000 | 0.000 | - | 确认随身物品 |
| 4 | `look` | 0.000 | 0.000 | - | 查看卧室 |
| 5 | `east` | 0.000 | 0.000 | - | 失败，提示先下床 |
| 6 | `check valid actions` | 0.000 | 0.000 | - | 返回当前可行动作 |
| 7 | `get up` | 0.000 | 0.143 | `You get out of bed.` | 实际命中第 1 个子目标 |
| 8 | `east` | 0.000 | 0.143 | - | 到客厅 |
| 9 | `south` | 0.000 | 0.143 | - | 前门关闭 |
| 10 | `examine front door` | 0.000 | 0.143 | - | 查看前门 |
| 11 | `check valid actions` | 0.000 | 0.143 | - | 返回可行动作 |
| 12 | `open front door` | 0.000 | 0.286 | `You open the front door.` | 实际命中第 5 个子目标 |
| 13 | `east` | 0.000 | 0.286 | - | 进厨房，方向选择对通关无帮助 |
| 14 | `west` | 0.000 | 0.286 | - | 回客厅 |
| 15 | `south` | 0.000 | 0.286 | - | 尝试出门，被“你看起来太狼狈”阻止 |
| 16 | `north` | 0.000 | 0.286 | - | 无效方向 |
| 17 | `west` | 0.000 | 0.286 | - | 回卧室 |
| 18 | `examine dresser` | 0.000 | 0.286 | - | 查看梳妆台 |
| 19 | `open dresser` | 0.000 | 0.286 | - | 找到干净衣服 |
| 20 | `take clean clothing` | 0.000 | 0.286 | - | 拿到干净衣服 |
| 21 | `check valid actions` | 0.000 | 0.286 | - | 查看此时可行动作 |
| 22 | `take off soiled clothing` | 0.000 | 0.286 | - | 脱掉脏衣服 |
| 23 | `take off all` | 0.000 | 0.286 | - | 连手表也脱了 |
| 24 | `put on clean clothing` | 0.000 | 0.286 | - | 失败，因为还没洗澡 |
| 25 | `check valid actions` | 0.000 | 0.286 | - | 查看此时可行动作 |
| 26 | `south` | 0.000 | 0.429 | `Bathroom ...` | 实际命中第 2 个子目标 |
| 27 | `examine sink` | 0.000 | 0.429 | - | 查看洗手池 |
| 28 | `look` | 0.000 | 0.429 | - | 查看浴室 |
| 29 | `check valid actions` | 0.000 | 0.429 | - | 结束前仍未洗澡 |

这条轨迹能清楚看出三件事：

1. 当前原始日志里的 `progress_rate` 始终是 `0.0`，并不是因为 agent 完全没有进展，而是因为标注前缀导致一个 milestone 都匹配不上。
2. 即使修正标注前缀，这一局也只完成了 3 个子目标：
   - 起床
   - 开前门
   - 到浴室
3. 这局真正没有完成的关键节点是：
   - 没有洗澡
   - 没有穿上干净衣服
   - 没有上车发动
   - 没有离开城市

因此，Jericho 当前的问题分成两层：

- 评测实现层：把应有的部分进展错误记成了 0
- agent 能力层：即使修正评分，这个 `905` episode 仍然不会成功

### 如何理解 `inventory` 在这个例子里的作用

在这条 `905` 轨迹里，第 3 步的 `inventory` 返回：

```text
You are carrying: some keys a wallet (which is closed) some soiled clothing (being worn) a gold watch (being worn)
```

它能帮助 agent 明确三件事：

1. 关键物品已经拿到：`keys`、`wallet`
2. 角色当前仍然穿着 `soiled clothing`
3. 角色还戴着 `gold watch`

后续从逻辑上说，agent 应该据此推断：

- 不能直接出门
- 需要先去浴室处理“脏”的状态
- 还要注意手表和随身物品可能影响洗澡动作

也就是说，`inventory` 更像是“状态确认工具”，帮助 agent 修正计划，而不是直接推动 reward 变化的打分动作。

### 适用范围、局限与推荐使用口径

下面这段话可以直接作为当前 Jericho 评测方式的对外说明：

> 当前项目中的 Jericho 评测采用基于标注 subgoal 的 observation 命中式评测：agent 与文本环境交互，评测器根据 observation 是否命中手写 milestone 来计算 `progress_rate`，并以所有 milestone 是否全部命中作为 `success_rate`。对于模型横向对比、prompt 或策略快速迭代、版本回归检查，这种方式可以直接复用，因为它实现成本低、解释性较强，并且在同一套标注和同一环境配置下具有稳定的相对比较价值。但它不应被理解为严格意义上的“游戏真实完成率”或唯一 benchmark 结论，因为评分依赖 observation 文本匹配，容易受到标注写法、文本前缀、表述差异以及无效动作判定粗糙度的影响。推荐使用口径是：将当前 Jericho 分数主要用于比较不同模型或不同 agent 版本在同一评测设置下的相对进展能力，并结合 `progress_rate`、`success_rate`、关键 episode 轨迹和 `score_change_record` 一起分析，而不要把单次 `success_rate` 直接等同于环境底层真实通关率。

补充理解：

- 在当前仓库里，`Subgoal n:` 前缀清洗问题已经修复，因此新的 Jericho 结果可以继续用于横向比较。
- 但只要底层仍然是 observation 文本匹配，Jericho 就天然比 PDDL 这类状态评测更脆弱。
- 如果未来要把 Jericho 用作更正式的最终 benchmark，建议再补强无效动作判定，并尽可能引入环境真实状态或更稳的完成信号做辅助校验。

### 与 PDDL 的区别

Jericho 是纯文本 observation 匹配。PDDL 是底层逻辑状态匹配。Jericho 的评测对标注格式非常敏感，PDDL 则不会受到 `Subgoal 1:` 这类文本前缀影响。

---

## 五、BabyAI

### 入口文件

- 任务入口：`agentboard/tasks/babyai.py`
- 环境入口：`agentboard/environment/babyai_env.py`
- 标注文件：`data/babyai/test.jsonl`

### 交互方式

BabyAI 底层是网格世界环境。Agent 看到的是文本化后的局部环境描述和目标，输出高层文本动作，例如移动、转向、拾取、开门等。

环境内部会把高层文本动作映射成若干低层动作执行。

### 动作合法性判断

BabyAI 环境维护当前 `action_space`。

判断逻辑：

- 如果 action 包含 `check`，返回当前可用动作列表，算有效。
- 如果 action 为空或不在 `action_space`，算无效。
- 如果 action 在 `action_space`，执行对应低层动作序列。
- 低层动作执行后会检查图像状态是否变化，用于判断动作是否真的生效。

`infos["action_is_valid"]` 会被 task 层用于统计 `grounding_acc`。

### progress_rate 判断

BabyAI 也使用 `obs_to_reward`。

加载逻辑：

- `babyai.py` 读取 `data/babyai/test.jsonl` 中的 `subgoals`。
- 如果 `subgoals_1` 等字段不存在，就直接把 `line["subgoals"]` 放入 `obs_to_reward`。
- 环境每步调用 `update_reward(new_obs)`。

匹配逻辑与 Jericho 不同。BabyAI 的 `match_style()` 支持 `*` 和 `**` 作为分隔符：

- 将 pattern 按 `**` 或 `*` 切成多个片段。
- 将 observation 按句号分句。
- 只要某一句 observation 同时包含所有片段，就算匹配。

### success_rate 判断

BabyAI 同时看环境 done 和 reward：

- 如果环境原生 `done=True`，但 reward 不超过 `0.5`，代码会把 done 改回 False，避免跳步误判。
- 如果 reward 已经等于 1，但环境还没 done，代码会强制 `done=True`。

因此 BabyAI 的 success 大体等价于：

```text
所有标注子目标命中，或环境完成且进度足够高
```

### 已知风险

`data/babyai/test.jsonl` 中也能看到 `Subgoal 1:` 前缀。当前代码没有明显清洗这个前缀，因此 BabyAI 也可能存在与 Jericho 类似的标注格式脆弱性。实际影响需要结合具体 observation 验证。

---

## 六、ALFWorld

### 入口文件

- 任务入口：`agentboard/tasks/alfworld.py`
- 环境入口：`agentboard/environment/alfworld/alfworld_env.py`
- 标注文件：`data/alfworld/test.jsonl`

### 交互方式

ALFWorld 是文本家居任务环境。Agent 根据目标和 observation 输出动作，例如：

```text
go to cabinet 1
open cabinet 1
take bowl 1 from cabinet 1
put bowl 1 in/on desklamp 1
```

task 层会做一个动作格式清洗：

- 如果包含 `put ... in ...` 或 `put ... on ...`，会统一改成 `put ... in/on ...`。
- 去掉结尾句号。

### 动作合法性判断

task 层通过：

```python
if action in self.env.get_action_space():
    grounding_acc_count += 1
```

统计动作是否在当前 valid action 列表中。

环境本身也支持：

- `look`
- `check valid actions`

### progress_rate 判断

ALFWorld 使用标注 subgoals 做正则匹配。

标注文件中的 subgoal 示例：

```text
Subgoal 1: ^(?=.* you see)(?=.*a bowl \d+)
Subgoal 2: You pick up the bowl \d+
Subgoal 3: ^(?=.* you see)(?=.*a desklamp)
```

环境每步对 observation 执行：

```python
re.search(pattern, observation)
```

命中一个 pattern，就把对应 `finished_sub_goal[i]` 置为 1。

reward 计算：

- 如果环境原生 `isdone=True`，reward 直接为 `1.0`。
- 否则 reward 是 `sum(finished_sub_goal) / len(finished_sub_goal)`。

### success_rate 判断

task 层以环境返回的 `done` 作为 success：

- `done=True`，单题 success 记 1。
- `done=False`，单题 success 记 0。

因此 ALFWorld 的 progress 可以来自子目标 regex 命中，但最终 success 仍主要依赖 ALFWorld 环境是否完成任务。

---

## 七、ScienceWorld

### 入口文件

- 任务入口：`agentboard/tasks/scienceworld.py`
- 环境入口：`agentboard/environment/scienceworld_env.py`
- 标注文件：`data/scienceworld/test.jsonl`

### 交互方式

ScienceWorld 是科学实验类文字环境。Agent 输出环境动作，例如移动、查看、拿取、聚焦、使用物品等。

每题加载流程：

1. 环境根据 label 中的 `task_name` 和 `var` 调用 `env.load()`。
2. `reset()` 返回初始 observation。
3. 初始 observation 会附加 inventory 信息。
4. Agent 输出动作，环境执行并返回 observation。

### 动作合法性判断

task 层使用：

```python
if action in self.env.get_action_space(abstract=False):
    grounding_acc_count += 1
```

来统计 `grounding_acc`。

`check valid actions` 会返回可用动作列表。

### progress_rate 判断

ScienceWorld 和 ALFWorld 类似，也是基于标注 subgoal 的 regex 匹配：

```python
re.search(pattern, observation)
```

每命中一个 pattern，就标记一个 finished subgoal。

reward：

```text
sum(finished_sub_goal) / len(finished_sub_goal)
```

### success_rate 判断

ScienceWorld 的 `done` 由环境封装层判断：

```text
所有 selected_obs 标注 subgoals 均已命中
```

因此 ScienceWorld 的 success 本质上是“全部正则子目标是否命中”。

---

## 八、WebShop

### 入口文件

- 任务入口：`agentboard/tasks/webshop.py`
- 环境入口：`agentboard/environment/webshop_env.py`
- Web 环境：`agentboard/environment/WebShop/...`

### 交互方式

WebShop 是文本化网页购物任务。

Agent 动作一般是：

```text
search[query]
click[item]
click[option]
click[Buy Now]
```

环境维护网页 session，将动作映射到 URL 请求或页面交互，然后把页面内容转换成文本 observation。

### 动作合法性判断

WebShop 环境解析 action 格式：

- `click[...]`
- `search[...]`

如果格式不正确，返回格式错误提示，`grounding=False`。

如果点击不存在的元素，可能抛出断言异常，task 层会提示只能点击 observation 中出现的按钮或链接。

task 层用环境返回的 `grounding` 统计 `grounding_acc`。

### progress_rate 判断

WebShop 不用 label 子目标文本匹配。

环境会从网页 HTML 中读取隐藏分数：

- `Your progress score (min 0.0, max 1.0)` 更新 `sub_reward`
- `Your score (min 0.0, max 1.0)` 更新最终 `reward`

task 层记录的 `progress_rate` 是 `self.env.sub_reward`，即历史最高 progress score。

### success_rate 判断

当页面到达终态，task 层判断：

```text
reward == 1
```

才算成功。

如果步数耗尽，则 success 为 0，progress 为当前 `sub_reward`。

---

## 九、WebArena / WebBrowse

### 入口文件

- 任务入口：`agentboard/tasks/webbrowse.py`
- 浏览器环境：`agentboard/environment/browser_env/envs.py`
- evaluator：`agentboard/environment/browser_env/evaluation_function.py`

### 交互方式

WebArena 使用真实浏览器页面。Agent 看到：

```text
WINDOWED PAGE:
...
URL: ...
```

Agent 输出浏览器动作，可能是：

- ID accessibility tree 动作
- Playwright 动作
- STOP 动作

具体格式由 `action_set_tag` 决定。

交互流程：

1. task 从 `data/webarena/test.jsonl` 转换出每个 config file。
2. browser env reset 到指定页面。
3. Agent 输出动作文本。
4. `extract_action()` 解析动作。
5. 根据 `action_set_tag` 转成浏览器动作。
6. Playwright 执行动作。
7. 环境记录页面状态、截图、trace、progress。

### 动作合法性判断

WebArena 的 `grounding_acc` 主要统计：

- 动作解析是否失败。
- 是否因重复动作或解析失败触发 early stop。
- 浏览器动作是否能正常执行。

如果 `extract_action()` 失败，会计入 grounding error。

### progress_rate 判断

WebArena 的 progress 由 evaluator 计算，不是简单文本匹配。

环境每步调用 `progress_evaluator_router(config_file)`，根据任务配置选择：

- URL progress evaluator
- Content progress evaluator
- URL + content 组合 evaluator

环境将计算得到的 `progress_score` 写入 `self.reward`。

### success_rate 判断

结束时 task 调用：

```python
evaluator_router(config_file)
```

得到最终 evaluator，再用浏览器轨迹和页面状态判定 success。

如果 `progress_score == 1.0`，代码会直接把 success 置为 1。

---

## 十、Tool 系任务

### 适用任务

`tool-query` 和 `tool-operation` 通过同一个 task：

- 任务入口：`agentboard/tasks/tool.py`

底层工具环境包括：

- `agentboard/environment/todo_env.py`
- `agentboard/environment/movie_env.py`
- `agentboard/environment/weather_env.py`
- `agentboard/environment/academia_env.py`
- `agentboard/environment/sheet_env.py`

### 交互方式

Agent 必须输出统一格式：

```text
Action: <action_name> with Action Input: <json-like input>
```

task 层调用：

```python
extract_action_name_and_action_input(message)
```

提取 action 和 action input。

如果格式错误，直接返回格式错误 observation，不执行环境动作。

### 通用动作合法性判断

每个 tool env 会解析 action：

```python
action_type, params = parse_action(action)
```

如果解析失败或 action_type 不支持，返回：

```text
ERROR | ...
```

task 层用 observation 是否以 `ERROR | ` 开头来统计 grounding：

- 不以 `ERROR | ` 开头，算有效动作。
- 以 `ERROR | ` 开头，算无效动作。

所有工具环境一般支持：

```text
check_valid_actions
finish
```

任务通常在 agent 调用 `finish` 后结束。

### todo / movie / weather / academia 的 progress 判断

这几类环境都有 `ground_truth_subgoals`，但不是拿 observation 文本去匹配。

它们会读取 agent 的 `action_path`，从中取出每步工具函数记录的：

```python
item["Subgoal"]
```

然后和 ground truth subgoals 做精确包含匹配：

```python
predicted_subgoals = [item["Subgoal"] for item in action_path]
count = 0
for subgoal in ground_truth_subgoals:
    if subgoal in predicted_subgoals:
        count += 1
reward = count / num_subgoals
```

`weather` 对经纬度字典有特殊相等判断，允许浮点近似或结构化比较。

因此 tool 系 progress 更像“工具调用路径覆盖了多少标准中间结果”。

### sheet 的 progress 判断

`sheet` 不使用 action_path subgoal 覆盖率，而是将当前表格状态与 ground truth JSON 做 match score：

```python
reward = get_match_score(observation_space, ground_truth)
```

reward 取历史最大值。

### success_rate 判断

task 层统一在结束后判断：

- 如果没有 `done`，success 为 False。
- 如果 `done=True`，多数工具要求 `reward == 1.0`。
- 对 `todo` 的部分问答型任务，还要求最终输出与 ground truth 完全相等。

简化：

```text
success = done and reward == 1.0
```

todo 特例：

```text
success = done and reward == 1.0 and final_output == ground_truth
```

---

## 十一、Neural 任务

### 入口文件

- 任务入口：`agentboard/tasks/neural.py`

### 交互与评测方式

`neural.py` 是一个更通用的 task wrapper：

1. 从 label 文件读取每个 env config。
2. 调用 `load_environment("neural", env_config)` 加载环境。
3. Agent 根据环境提供的 goal 和 observation 输出 action。
4. 环境返回 `state, reward, done, infos`。
5. task 层直接使用环境返回的 `reward` 作为 progress，`done` 作为 success。

当前仓库中未在 `agentboard/environment` 下看到同名的显式 `neural_env.py` 文件，因此这个任务的具体判断逻辑需要结合实际注册的 neural environment 查看。就 task 层而言，它没有额外的文本匹配或 evaluator 判断。

---

## 十二、不同任务的判断方式对照

| 任务 | 动作形式 | 动作有效性 | progress_rate | success_rate | label_path 作用 |
|---|---|---|---|---|---|
| PDDL | 规划动作文本，如 `stack b1 b2` | 能解析成当前合法 PDDL action 且状态变化 | 当前状态满足 goal literals 的比例，取历史最大 | 所有 goal literals 同时满足 | 主要读取 difficulty，不用 subgoals 评分 |
| Jericho | 文字冒险命令，如 `open door` | 当前实现主要看是否含 `[!`，较粗糙 | observation 匹配标注 subgoals 的比例 | 所有标注 subgoals 命中 | subgoals 是核心评分依据 |
| BabyAI | 网格世界高层动作 | action 是否在 action_space，低层动作是否生效 | observation 片段匹配标注 subgoals | reward=1 或环境 done 且进度足够 | subgoals 是核心评分依据 |
| ALFWorld | 家居文本动作 | action 是否在当前 valid action list | regex subgoals 命中比例，done 时为 1 | 环境 done | subgoals regex 用于 progress |
| ScienceWorld | 科学实验动作 | action 是否在 valid action list | regex subgoals 命中比例 | 全部 subgoals 命中 | subgoals regex 用于 progress 和 done |
| WebShop | `search[]` / `click[]` | 页面动作格式和目标元素是否有效 | 网页隐藏 progress score | 最终网页 score 是否为 1 | 主要用于 difficulty |
| WebArena | 浏览器动作 | action 解析和浏览器执行是否成功 | URL/content evaluator progress | evaluator 最终判断，或 progress=1 | 任务 config/evaluator 来源 |
| Tool | `Action: ... with Action Input: ...` | parse_action 成功且 observation 非 ERROR | action_path 覆盖 ground truth subgoals，sheet 为状态 match | done 且 reward=1，todo 部分还比最终输出 | 提供 goal、ground truth、subgoals、difficulty |

---

## 十三、容易混淆的问题

### 1. `progress_rate = 1.0` 是否一定 success

不一定，取决于任务。

- PDDL 中如果当前已经满足全部 goal literals，通常会 success。
- WebArena 中代码会把 `progress_score == 1.0` 视为 success。
- Tool 中还要求 `done=True`，todo 问答型还要求最终输出完全等于 ground truth。
- ALFWorld 中 progress 可以部分来自 regex 命中，最终 success 仍看环境 done。

### 2. `grounding_acc` 是否表示任务做对

不是。

`grounding_acc` 只表示动作是否被环境接受或能落地。它不代表策略正确。例如 PDDL 中反复执行合法但无助于目标的动作，grounding 可以高，但 success 仍然低。

### 3. `score_change_record` 为什么有时为空

如果整局 reward 从未上升，就为空。Jericho 当前三次全 0 的日志中 `score_change_record=[]`，是因为标注前缀导致没有任何 subgoal 被匹配。

### 4. `label_path` 是否都参与评分

不是。

- PDDL: 基本只用 difficulty。
- Jericho/BabyAI/ScienceWorld/ALFWorld: subgoals 直接参与 progress 或 done。
- WebShop/WebArena: 主要用于任务配置和难度，评分来自网页/evaluator。
- Tool: 提供 ground truth、标准中间结果和难度。

### 5. 为什么 Jericho 比 PDDL 更容易出现全 0

PDDL 使用结构化逻辑状态，不依赖标注文本前缀。Jericho 使用 observation 子串匹配，标注中的 `Subgoal 1:` 前缀、标点、大小写、观察文本变化都可能导致匹配失败。

---

## 十四、建议排查顺序

如果某个任务分数异常，建议按以下顺序看：

1. 看 `*.txt` 摘要：确认是 success 低、progress 低，还是 grounding 低。
2. 看 `logs/*.jsonl` 完整轨迹：确认每一步 action、observation、progress。
3. 对 PDDL：检查 goal literals 是否真的被满足，别只看自然语言。
4. 对 Jericho/BabyAI/ScienceWorld/ALFWorld：检查标注 pattern 是否能在真实 observation 中匹配。
5. 对 WebShop/WebArena：检查页面/evaluator 是否返回 progress 或 reward。
6. 对 Tool：检查 action 格式、observation 是否 `ERROR`、action_path 是否记录了标准 subgoals。

---

## 十五、当前已知实现风险

### Jericho 标注前缀问题

这个问题已经在当前仓库代码中修复：`EvalJericho.load_annotation()` 现在会先去掉 `Subgoal n:` 前缀，再把文本送入 `obs_to_reward`。因此新跑出来的 Jericho 结果不应再出现“明明命中了 milestone 但 progress 仍然始终为 0”的前缀污染问题。

### Jericho 动作有效性偏乐观

当前只用 `'[!' in observation` 判断动作无效，很多自然语言失败反馈不会被计为无效，导致 `grounding_acc` 偏高。

### BabyAI 也可能有标注前缀风险

`data/babyai/test.jsonl` 同样存在 `Subgoal n:` 前缀，且当前代码未明显清洗。需要用真实轨迹离线验证是否影响匹配。

### PDDL progress 是历史最大值

PDDL 的 progress 不会下降。分析失败轨迹时，不能把最终 progress 当作最终状态的精确满足比例，它表示本局曾经达到过的最好进度。

### Tool 系 success 可能比 progress 更严格

Tool 任务中 reward 达到 1 还不一定足够，某些 todo 问答型任务还要求最终输出与 ground truth 完全相等。
